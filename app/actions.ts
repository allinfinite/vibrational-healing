'use server';

// Dynamic generation is disabled in favor of pre-generation script
// See scripts/generate-assets.js

export async function generateSpeech(text: string) {
  // Placeholder if needed later
  return null;
}

export async function generateIconSvg(concept: string): Promise<string | null> {
  // Placeholder if needed later
  return null;
}
